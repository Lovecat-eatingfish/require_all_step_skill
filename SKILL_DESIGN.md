# Skill 设计文档：feature-delivery

> 需求驱动开发全流程 Skill。
> 本文档描述 skill 的完整逻辑，确认后落地为 SKILL.md + 配套子文档。

---

## 1. 一句话定位

当用户抛来一个"需求"（产品需求、功能描述、bug 修复要求等），本 skill 引导 AI 按照
**读代码 → 读需求 → 对齐核心问题 → 开发（持续对齐）→ 测试 → 交付** 的流程高质量地完成开发，
而不是拿到需求就直接闷头写代码。

核心价值：把"高级工程师接到需求后的标准动作"固化成流程，让 AI 每一步都不漏、不猜、不糊弄。

---

## 2. 触发时机

**用户主动触发**。当用户明确要"开发一个功能"或"实现一个需求"时使用。

适用场景：
- "帮我实现这个需求：……"
- "根据这份 PRD / 需求文档开发……"
- "把这个问题修一下 / 加个 XX 功能"

不适用场景（不触发）：
- 纯问答、纯咨询类请求（不涉及写代码）
- 用户没有主动要求走流程
- 一句话能搞定的微调（如"把按钮颜色改成红色"）

---

## 3. 复杂度判断

在开始工作前，AI 先根据需求内容判断复杂度，然后向用户确认：

| 等级 | 特征 | 流程深度 |
|------|------|----------|
| 简单 | 单文件/单模块改动，逻辑清晰，无歧义 | 快速走完，对齐环节精简 |
| 普通 | 多文件改动，涉及 2-3 个模块，有一定歧义 | 标准流程 |
| 复杂 | 跨多模块，改公共接口，有性能/安全影响 | 完整流程，对齐更深入 |

AI 判断后告知用户："我判断这次需求是 [简单/普通/复杂] 级别，按 [精简/标准/完整] 流程走，可以吗？"
用户可以调整。

---

## 4. 总体流程

```
┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐
│ Step 1  │ → │ Step 2  │ → │ Step 3  │ → │ Step 4  │ → │ Step 5  │
│ 读代码  │ → │ 读需求  │ → │ 对齐    │ → │ 开发    │ → │ 测试交付│
└─────────┘   └─────────┘   └─────────┘   └─────────┘   └─────────┘
                              ↑              │
                              └──────────────┘
                              开发中遇到问题
                              继续对齐
```

关键原则：
- **Step 3 是前置关卡**：至少确认核心三要素后才能动手写代码。
- **开发中持续对齐**：遇到不明确的问题随时提问，不追求"一次对齐所有细节"。
- **测试务实**：能跑测试就跑，不能跑就做静态验证，不假装能做所有事。

---

## 5. 各步骤详细逻辑

### Step 1：代码库预览（Codebase Recon）

**目标**：像新入职的工程师一样，先搞清楚项目的"地形"。

**深度**：中等——建立对项目整体架构的理解，重点看与需求相关的部分。

**动作**：
1. **项目概览**：读 README、目录结构、构建/运行方式、技术栈。
2. **架构摸底**：识别分层结构（如 controller/service/dao、页面/逻辑/接口），找出
   需求涉及的模块位置。
3. **相似实现搜索**：在代码库里找与需求功能点相似的既有实现，作为开发的"惯例参照"
   （命名风格、错误处理方式、目录放置位置都跟着项目走）。
4. **关联方识别**：找出会被本次改动影响的上下游（调用方、被调用方、配置、文档）。
5. **约束发现**：注意到项目的特殊约定——代码规范、测试框架、是否有 AGENTS.md / CLAUDE.md。

**产出**：代码库概览小结（技术栈、目录结构中与需求相关的部分、参照实现）。

---

### Step 2：需求阅读与结构化（Requirement Intake）

**目标**：把用户给的需求"读懂、拆开、列出疑点"。

**动作**：
1. 完整阅读用户提供的需求材料（对话描述、PRD 文档、issue 链接、截图等）。
2. 输出一份**结构化的需求理解卡片**：
   - **需求目标**：这个需求要达成什么业务效果（用自己的话复述）
   - **功能点清单**：拆成编号的功能点 F1、F2……（后续开发和测试都按编号追踪）
   - **涉及的模块猜测**：初步判断会动到哪些模块/接口/页面
   - **疑点清单（Q-list）**：所有模糊、有歧义、缺失的地方，逐条编号（Q1、Q2……）
3. 标注哪些疑点已经在 Step 1 看代码时得到了解答（依据：xxx 文件）。

**产出**：需求理解卡片（含功能点清单 + 疑点清单）。

---

### Step 3：需求对齐（Alignment）

**目标**：确认核心三要素，拿到开发的基本口径。**三要素缺一不可。**

**核心三要素**（必须在写代码前确认）：
1. **需求目标**：要做什么，达成什么效果
2. **验收标准**：怎样算完成，有哪些必须覆盖的场景
3. **不做什么**：明确 out of scope，避免范围蔓延

**可选对齐内容**（可以在开发中再定）：
- 边界条件的具体行为（列表为空时显示什么？）
- 异常处理策略（网络失败时重试还是提示？）
- 具体的 UI 细节（间距、颜色、动画）

**提问方式**：使用 AskUserQuestion 工具（结构化选项式提问），分批提问，每批不超过 3-4 个问题。

**对齐确认单**（开发前给用户最后看一眼）：
- 最终功能点清单（含每个功能点的行为口径）
- 已确认的边界/异常处理策略
- 明确不做的事项
- 开发计划（动哪些文件/模块，按什么顺序）

用户确认后进入 Step 4。若用户说"别问了，直接干"，至少强制确认三要素，其余按最优假设处理，
在交付时明确列出这些假设。

---

### Step 4：代码开发（Implementation）

**目标**：按对齐确认单实施，写"符合这个项目惯例"的代码。

**动作**：
1. **先定方案后动手**：复杂改动先向用户说明实现方案再动手；简单改动直接做。
2. **按功能点编号实施**：对照 F1、F2……逐个实现，不漏项。
3. **遵循项目惯例**：命名、目录、错误处理、注释密度向 Step 1 找到的参照实现看齐。
4. **增量可验证**：每完成一个功能点，确保代码在语法/编译层面完整。
5. **同步文档**：如果项目有接口文档/README 中对应部分需要更新，一并更新。
6. **遇到不明确的问题**：随时向用户提问，继续对齐。不猜测，不硬撑。
7. **小步提交建议**：如项目用 git，按功能点分组给用户建议 commit 划分（不主动 commit）。

**产出**：完成的代码改动 + 改动清单（改了哪些文件、每个文件为什么改）。

---

### Step 5：测试与交付（Test & Deliver）

**目标**：务实验证 + 清晰交付。

#### 5.1 测试

**原则**：不假装能做所有事，诚实标注能力边界。

**能做的**：
- 有测试框架 → 跑测试（单元测试、集成测试）
- 没有测试框架 → 做代码审查级别的静态验证（逻辑覆盖、边界检查、错误路径）
- 设计测试用例（正常流、边界值、异常流），写成可执行的测试代码

**不能做的**：
- 需要真实运行环境的集成测试（后端服务、数据库、第三方 API）
- 需要人工操作的 UI 测试
- 性能测试、安全测试

**不能做的部分**：明确列出"需人工验证"的清单，包含验证步骤和预期结果。

#### 5.2 交付总结

**产出（最终一条消息内）**：
1. **需求与实现对照**：F1~Fn 每个功能点是怎么实现的（一句话/点）
2. **改动清单**：新增/修改/删除的文件及原因
3. **测试报告**：测了什么、结果如何、哪些无法自测需人工验证
4. **遗留假设与风险**：所有"我替你做的假设"、潜在风险点
5. **验收建议**：给用户的最短验收路径（按哪几步手动点一遍即可确认）

---

## 6. 与原设计的主要差异

| 维度 | 原设计 | 新设计 |
|------|--------|--------|
| 流程结构 | 六阶段线性 | 五步简化流程 |
| 提问时机 | Phase 1 不提问，Phase 3 集中提问 | 即时对齐，核心前置 + 细节后置 |
| 对齐机制 | 硬性关卡，所有疑点解决才动手 | 三要素确认即可动手，开发中持续对齐 |
| 测试定位 | "专业自测（测试工程师视角）" | 务实定位，承认局限 |
| 复杂度 | 无区分 | AI 判断 + 用户确认 |
| 触发方式 | 自动触发 | 用户主动触发 |
